{% extends 'base.html.twig' %}

{% block title %}Home
{% endblock %}

{% block body %}
	<section class="home" aria-label="Accueil" id="home">
		<div class="title">
		<h1 class="text-center bg-secondary text-primary m-auto p-5 mt-5"> Bienvenue sur mon Portfolio </h1>
	</div>
</section>
<section class="qui-suis-je" aria-label="Qui-suis-je" id="qui-suis-je">
	<h1 class="text-center bg-secondary text-primary m-auto p-1 mt-5">
		Qui-suis-je
	</h1>

	{% for object in content %}
		<div class="information">
			<img class="w-25" src="{{ asset(object.photo) }}" alt="Photo">
			<p class="text-primary bg-secondary">{{ object.information }}</P>
		</div>
		<div class="content">
			<p class="text-primary bg-secondary">{{ object.content }}</P>
		</div>
	{% endfor %}
</section>

<section class="project d-flex flex-column align-items-center" aria-label="Project" id="project">
	{% for project in projects %}
		<div class="description">
			<p class="text-primary bg-secondary mt-3">{{ project.description }}</P>
		</div>
		{% for tag in project.Tags %}
			<p class="text-secondary bg-primary mt-3">{{ tag.language }}</p>
		{% endfor %}

		<iframe class="mb-5" src="{{ project.url }}" width="75%" height="500"></iframe>
		<a class="text-primary bg-secondary mb-5" href="{{ project.url }}">
			Lien du déploiement
		</a>
	{% endfor %}
</section>
<section class="contact d-flex flex-column align-items-center bg-secondary text-primary" aria-label="Contact" id="contact">
	{{ form_start(contactForm) }}
	{{ form_widget(contactForm) }}
	<button type="submit">Envoyer</button>
	{{ form_end(contactForm) }}
</section>{% endblock %}
